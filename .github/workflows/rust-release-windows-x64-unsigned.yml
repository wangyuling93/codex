# Fork-local Windows Intel/AMD x64 release: unsigned MSVC EXEs only.
#
# Separate from the canonical multi-platform `rust-release.yml` /
# `rust-release-windows.yml` (private runners + signing).
#
# Zero-config: no secrets, environments, or code signing. Uses only the
# automatic GITHUB_TOKEN when (optionally) publishing a GitHub Release.
#
# Output:
#   codex-x86_64-pc-windows-msvc.zip
#     (codex.exe + codex-code-mode-host.exe)
#
# Run:
#   Actions → rust-release-windows-x64-unsigned → Run workflow
#   Default: upload Actions artifacts only (no GitHub Release / tag).
#   Leave release_version empty to use the latest stable openai/codex release.
#   Enable "Publish GitHub Release" for a prerelease tag:
#     windows-x64-unsigned-v{resolved release version}
#
# Windows SmartScreen may warn on first run (unsigned). Use More info → Run anyway.

name: rust-release-windows-x64-unsigned

on:
  workflow_dispatch:
    inputs:
      publish_github_release:
        description: Create a GitHub prerelease (and tag) from the built assets
        required: false
        type: boolean
        default: false
      release_version:
        description: >-
          Version embedded in the binaries. Leave empty to use the latest
          stable openai/codex release (for example, 0.144.6).
        required: false
        type: string
        default: ""
      tag:
        description: >-
          Optional release tag (windows-x64-unsigned-vX.Y.Z[...]). Its version
          is used when release_version is empty. Otherwise the two must match.
        required: false
        type: string
        default: ""

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.release_version || inputs.tag || 'latest-stable' }}
  cancel-in-progress: false

jobs:
  resolve-version:
    runs-on: ubuntu-latest
    outputs:
      release_tag: ${{ steps.resolve.outputs.release_tag }}
      release_version: ${{ steps.resolve.outputs.release_version }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Resolve release version and tag
        id: resolve
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          INPUT_RELEASE_VERSION: ${{ inputs.release_version }}
          INPUT_TAG: ${{ inputs.tag }}
          TAG_PREFIX: windows-x64-unsigned-v
        run: |
          set -euo pipefail

          source_version="$(
            python3 - <<'PY'
          from pathlib import Path
          import re
          import sys

          text = Path("codex-rs/Cargo.toml").read_text()
          match = re.search(
              r"(?ms)^\[workspace\.package\]\s*(.*?)^\[",
              text + "\n[",
          )
          if match is None:
              sys.exit("could not find [workspace.package] in codex-rs/Cargo.toml")
          section = match.group(1)
          version_match = re.search(r'(?m)^version\s*=\s*"([^"]+)"\s*$', section)
          if version_match is None:
              sys.exit("could not find version in [workspace.package]")
          print(version_match.group(1))
          PY
          )"

          version_re='[0-9]+\.[0-9]+\.[0-9]+(-(alpha(\.[0-9]+){0,2}|beta(\.[0-9]+)?))?'

          if [[ -n "${INPUT_RELEASE_VERSION}" ]]; then
            release_version="${INPUT_RELEASE_VERSION}"
            version_source="workflow input"
          elif [[ -n "${INPUT_TAG}" ]]; then
            [[ "${INPUT_TAG}" == "${TAG_PREFIX}"* ]] \
              || { echo "❌  Tag '${INPUT_TAG}' must start with ${TAG_PREFIX}"; exit 1; }
            release_version="${INPUT_TAG#"${TAG_PREFIX}"}"
            version_source="workflow tag input"
          elif [[ "${source_version}" != "0.0.0" ]]; then
            release_version="${source_version}"
            version_source="Cargo.toml"
          else
            upstream_tag="$(gh api repos/openai/codex/releases/latest --jq .tag_name)"
            [[ "${upstream_tag}" =~ ^rust-v${version_re}$ ]] \
              || { echo "❌  Latest openai/codex release tag '${upstream_tag}' is unsupported"; exit 1; }
            release_version="${upstream_tag#rust-v}"
            version_source="latest stable openai/codex release"
          fi

          [[ "${release_version}" =~ ^${version_re}$ ]] \
            || { echo "❌  Release version '${release_version}' is unsupported"; exit 1; }
          [[ "${release_version}" != "0.0.0" ]] \
            || { echo "❌  Refusing to publish or embed the development placeholder 0.0.0"; exit 1; }

          if [[ -n "${INPUT_TAG}" ]]; then
            release_tag="${INPUT_TAG}"
          else
            release_tag="${TAG_PREFIX}${release_version}"
          fi

          pattern="^${TAG_PREFIX}${version_re}$"
          [[ "${release_tag}" =~ ${pattern} ]] \
            || { echo "❌  Tag '${release_tag}' must match ${TAG_PREFIX}X.Y.Z[...]"; exit 1; }

          tag_ver="${release_tag#"${TAG_PREFIX}"}"
          [[ "${tag_ver}" == "${release_version}" ]] \
            || { echo "❌  Tag version ${tag_ver} ≠ release version ${release_version}"; exit 1; }

          {
            echo "release_tag=${release_tag}"
            echo "release_version=${release_version}"
          } >> "${GITHUB_OUTPUT}"

          echo "✅  Codex ${release_version} from ${version_source}; release tag ${release_tag}"

  build:
    needs: resolve-version
    name: Build unsigned Windows x64 EXEs
    # Free GitHub-hosted Windows (Intel/AMD x64). Not private runner labels.
    runs-on: windows-latest
    timeout-minutes: 180
    permissions:
      contents: read
    defaults:
      run:
        working-directory: codex-rs
        shell: bash
    env:
      CARGO_NET_GIT_FETCH_WITH_CLI: "true"
      TARGET: x86_64-pc-windows-msvc
      # Original fork-local CLI pair.
      BINARIES: "codex codex-code-mode-host"
      STAGE_DIR: dist/release-assets
      # Match official Windows x64 sqlite build flag.
      LIBSQLITE3_FLAGS: SQLITE_DISABLE_INTRINSIC

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Set Codex build version
        shell: bash
        env:
          RELEASE_VERSION: ${{ needs.resolve-version.outputs.release_version }}
        run: |
          set -euo pipefail
          if command -v python3 >/dev/null 2>&1; then
            python_cmd=python3
          else
            python_cmd=python
          fi

          "$python_cmd" - "$RELEASE_VERSION" <<'PY'
          from pathlib import Path
          import re
          import sys

          version = sys.argv[1]
          cargo_toml = Path("Cargo.toml")
          text = cargo_toml.read_text(encoding="utf-8")
          section = re.search(
              r"(?ms)^\[workspace\.package\]\s*(.*?)(?=^\[|\Z)",
              text,
          )
          if section is None:
              raise SystemExit("could not find [workspace.package] in Cargo.toml")
          version_match = re.search(
              r'(?m)^(version\s*=\s*")([^"]+)("\s*)$',
              section.group(1),
          )
          if version_match is None:
              raise SystemExit("could not find [workspace.package].version")

          old_version = version_match.group(2)
          start = section.start(1) + version_match.start(2)
          end = section.start(1) + version_match.end(2)
          cargo_toml.write_text(text[:start] + version + text[end:], encoding="utf-8")
          print(f"Set [workspace.package].version from {old_version} to {version}")
          PY

      - name: Verify Windows runner arch
        shell: pwsh
        run: |
          $arch = [System.Runtime.InteropServices.RuntimeInformation]::OSArchitecture
          Write-Host "OSArchitecture=$arch"
          Write-Host "OSVersion=$([System.Environment]::OSVersion.VersionString)"
          if ($arch -ne 'X64') {
            throw "Expected X64 Windows runner, got $arch"
          }

      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          targets: x86_64-pc-windows-msvc

      - name: Configure MSVC environment
        uses: ./.github/actions/setup-msvc-env
        with:
          target: x86_64-pc-windows-msvc

      # Cache crate downloads only (~2–4GB). Do NOT cache codex-rs/target.
      - name: Cache cargo registry and git deps
        id: cargo-deps-cache
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: ${{ runner.os }}-cargo-deps-${{ hashFiles('codex-rs/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-deps-

      - name: Report cargo deps cache status
        shell: bash
        run: |
          set -euo pipefail
          echo "cargo-deps cache-hit=${{ steps.cargo-deps-cache.outputs.cache-hit }}"
          du -sh ~/.cargo/registry ~/.cargo/git 2>/dev/null || true

      # Windows MSVC uses the v8 crate's default upstream prebuilts (not
      # Codex Darwin/Linux RUSTY_V8_ARCHIVE pairs from setup-rusty-v8).

      - name: Cargo build (release)
        shell: bash
        run: |
          set -euo pipefail
          build_args=()
          for binary in ${BINARIES}; do
            build_args+=(--bin "$binary")
          done
          cargo build --locked --target "${TARGET}" --release "${build_args[@]}"

      - name: Verify embedded Codex version
        shell: bash
        env:
          RELEASE_VERSION: ${{ needs.resolve-version.outputs.release_version }}
        run: |
          set -euo pipefail
          version_output="$("target/${TARGET}/release/codex.exe" --version)"
          echo "$version_output"
          [[ "$version_output" == *"${RELEASE_VERSION}"* ]] \
            || { echo "Expected codex.exe --version to contain ${RELEASE_VERSION}" >&2; exit 1; }

      - name: Stage portable zip
        shell: bash
        run: |
          set -euo pipefail

          release_dir="target/${TARGET}/release"
          stage="${STAGE_DIR}"
          zip_name="codex-${TARGET}.zip"
          zip_root="${RUNNER_TEMP}/codex-windows-zip"

          rm -rf "$stage" "$zip_root"
          mkdir -p "$stage" "$zip_root"

          for binary in ${BINARIES}; do
            exe_path="${release_dir}/${binary}.exe"
            if [[ ! -f "$exe_path" ]]; then
              echo "Binary ${exe_path} not found" >&2
              ls -la "$release_dir" || true
              exit 1
            fi
            # Flat names for the zip (easy to run after extract).
            cp "$exe_path" "${zip_root}/${binary}.exe"
          done

          # Portable zip for download-and-run (unsigned).
          (
            cd "$zip_root"
            # Git Bash on Windows includes zip; fall back to PowerShell if needed.
            if command -v zip >/dev/null 2>&1; then
              zip -9 "${GITHUB_WORKSPACE}/codex-rs/${stage}/${zip_name}" ./*.exe
            else
              powershell.exe -NoProfile -Command \
                "Compress-Archive -Path '*.exe' -DestinationPath '${GITHUB_WORKSPACE}/codex-rs/${stage}/${zip_name}' -Force"
            fi
          )

          echo "Staged release assets:"
          ls -lah "$stage"

      - name: Upload release assets
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: windows-x64-unsigned
          path: codex-rs/dist/release-assets/codex-x86_64-pc-windows-msvc.zip
          if-no-files-found: error
          retention-days: 14

  publish:
    if: ${{ inputs.publish_github_release }}
    needs:
      - resolve-version
      - build
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Download release assets
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: windows-x64-unsigned
          path: release-assets

      - name: Verify zip present
        shell: bash
        run: |
          set -euo pipefail
          zip="release-assets/codex-x86_64-pc-windows-msvc.zip"
          if [[ ! -f "$zip" ]]; then
            echo "❌  ${zip} not found" >&2
            ls -lahR release-assets || true
            exit 1
          fi
          ls -lah release-assets/

      - name: Release notes
        id: notes
        shell: bash
        run: |
          set -euo pipefail
          notes_path="${RUNNER_TEMP}/release-notes.md"
          {
            echo "Unsigned Windows x64 (\`x86_64-pc-windows-msvc\`) build."
            echo
            echo "Primary asset: \`codex-x86_64-pc-windows-msvc.zip\`"
            echo
            echo "Contents: \`codex.exe\` and \`codex-code-mode-host.exe\` (unsigned)."
            echo "SmartScreen may warn on first run — More info → Run anyway."
            echo
            echo "Built from \`${GITHUB_SHA}\`."
            echo
            git log -1 --format=%B "${GITHUB_SHA}^{commit}"
          } > "${notes_path}"
          echo "path=${notes_path}" >> "${GITHUB_OUTPUT}"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@153bb8e04406b158c6c84fc1615b65b24149a1fe # v2.6.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          name: ${{ needs.resolve-version.outputs.release_version }} (windows-x64-unsigned)
          tag_name: ${{ needs.resolve-version.outputs.release_tag }}
          target_commitish: ${{ github.sha }}
          body_path: ${{ steps.notes.outputs.path }}
          files: release-assets/*
          overwrite_files: true
          fail_on_unmatched_files: true
          make_latest: false
          prerelease: true
